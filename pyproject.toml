[project]
name = "mechanism-interferometry-build"
version = "0.1.0"
description = "Reproducible paper, figure, example-data, and repository validation tooling"
requires-python = ">=3.12"
dependencies = [
  "jsonschema>=4.26,<5",
  "lxml>=6.0,<7",
  "matplotlib>=3.10,<4",
  "numpy>=2.3,<3",
]

[tool.uv]
package = false
