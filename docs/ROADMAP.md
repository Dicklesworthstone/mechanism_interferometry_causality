# Roadmap

## Packet 0: repository and exact algebra

- paper, website, schemas, and exact simulations;
- `mic-core` curvature and conservation identities;
- `mic-design` product-odds and rank audit;
- `mic-stats` parsimony-frontier localization primitive;
- `mic-engine` estimator lens-battery sensitivity screen;
- deterministic CLI simulation output;
- CI and repository integrity checks.

## Packet 0A: certificate-authority hardening

This packet is a prerequisite for any new `passed` path. Numeric primitives and
diagnostics may continue to ship as diagnostic-only while it is open.

Foundations already landed here include the hard product-odds tolerance ceiling,
blocking four-law geometry gates, diagnostic-only unvalidated selection,
manifest column-role and regime-alias validation, fail-closed zero-scale
reference multiplier bounds, and the histogram report's non-certifying status
schema. The remaining work is the authority-bearing integration below.

- replace `EvidenceLedger::status(bool)` with a typed gate aggregate that
  distinguishes satisfied, refuted, and missing evidence for locality,
  conditional normalization, flatness, selection, product design, overlap,
  clustering, equivalence calibration, and confirmation-split independence;
- make finalized ledgers append-only, reject duplicate provenance keys, and bind
  every stochastic stage's seed, randomization unit, folds, and source fingerprint;
- bind preflight product-odds and reweighting audits to the manifest, allocation
  artifact, and analyzed data so a syntactically valid audit reference cannot be
  transplanted to a different projection;
- require cluster-aware GCM standard errors and carry the declared randomization
  unit through bounds, orientation, and the final report;
- audit every primitive ratio on every observed face and bind the reported
  overlap summary to that complete collection, rather than retaining only the
  first successfully projected face;
- propagate hard abstention semantics for partial common support and externally
  supplied degenerate bounds through every library/report boundary;
- encode the remaining impossible authority combinations in the general report
  schema (for example, exploratory `passed` or `passed` with blocking findings)
  and add negative schema fixtures.

## Packet 1: tabular vertical slice

- FrankenPandas manifest ingestion;
- stable row, cluster, and fold IDs;
- overlap and selection audit;
- JSON/Parquet evidence bundle.

## Packet 2: two-sample equivalence

- MMD and energy U-statistics (reference forms in `mic-stats`);
- cluster-aware multiplier/bootstrap intervals (mean-type reference: `mic_stats::simultaneous_mean_bounds`; U-statistic corrections outstanding);
- relative deletion equivalence (`mic_stats::classify_deletion`);
- pass-count state machine and golden parity fixtures (`mic_stats::orient_from_deletions`, `mic_engine::audit_orientation`, derived parity pass count, common-tolerance guard, and calibrated CLI input schema/provenance);
- overlap gate on ratio-weight effective sample size (`mic_engine::audit_overlap`);
- active tilt proposal for multiple-pass disambiguation, with executable CLI, input/output schemas, candidate-library fingerprint, and explicit no-eligible-candidate abstention (quarantined in `mic-proposal`).

## Packet 3: GCM/wGCM

- product-sampling gate and reweighting;
- syntax- and arithmetic-validating product-design references on every GCM projection (`mic_stats::ProductDesignEvidence`), followed by the content-bound engine resolution required in Packet 0A;
- cross-fitted nuisance regressions;
- fixed and weighted witness tests;
- adaptive split witness;
- estimator lens battery wired to real projections;
- non-product quota conformance failure.

## Packet 4: joint multinomial ratios

- FrankenTorch main-effect and interaction models;
- sampling-offset and intercept contract;
- posterior calibration;
- curvature-field export;
- held-out proper-loss diagnostics.

## Packet 5: compositional prediction

- primitive ratio normalization;
- raw and self-normalized products;
- held-out combination scoring;
- effective sample-size and truncation sensitivity.

## Packet 6: localization and orientation

- Design-IAMB;
- stability selection;
- parsimony-frontier ensemble localization over sampled candidate supports;
- deletion audit integration;
- family peeling and graph assembly.

## Packet 7: state expansion and representations

- nested curvature comparisons;
- design-sufficiency probes;
- candidate block search;
- invertible and sufficient representation conformance.

## Packet 8: empirical pilots

- feature-flag pilot;
- Perturb-seq ingestion and clustered analysis;
- CPA-style comparison;
- public reproducibility bundle after review.

## Follow-up research

- fractional-factorial optimal design for function-valued curvature;
- Brillinger partition formulas for higher-order latent conservation;
- U-statistic interaction-lattice estimators;
- transition-level mechanism interferometry for feedback systems.
