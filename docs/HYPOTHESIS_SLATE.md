# Hypothesis slate (mail-swarm only)

This is the Brenner Step-0 artifact for the *existing* Agent Mail swarm.
It is not an NTM session, not a beads workspace, and not a certificate.

**Governing law (frontier-math, mapped onto MIC):** a label, schema field, or
`certificate_status` value does not create a modular soft-intervention
representation. Locality, conditional normalization, and square flatness do.

**Authority of this file:** `proposal_only`. It may decide what to test next.

## Question of record

**Q-001.** Can mechanism interferometry recover *audited* causal structure from
a large unlabeled table that contains factorial-ish columns, without a
preregistered pair list, DAG, or outcome of interest?

**Falsifier.** The answer is no, in this repo, if any of the following is
observed on `examples/data/four_law_discrete.csv` or a later Norman extract:

1. `run_unsupervised_survey` fails to propose the complete encoded square; or
2. `run_tabular_audit` or the survey serializes `certificate_status: passed`; or
3. a target is inferred when the deletion pass-count is not exactly one; or
4. cells/requests are treated as independent when `cluster_column` is a higher unit.

**Current status of the falsifier suite.** (1) failed to fire: survey finds
`regime` as a complete square. (2) failed to fire: four-law reports `abstained`.
(3)–(4) are enforced by `audit_orientation` and `cluster_spans_regimes`. So Q-001
is **not confirmed**. It is only **not yet killed on the toy square**.

## Hypotheses (including a third alternative)

Score = (mind-change × falsifiability × option value) / (cost × ambiguity × infra).
Advance only if ≥ 2.

| ID | Claim | Falsifier | Expected evidence | Origin | Score | State |
|---|---|---|---|---|---|---|
| H-001 | Stage 0–1 survey + histogram four-law + a *declared* selection contract + confirmation-cluster orientation is enough to name a UNIQUE_TARGET on a real Norman pair. | On a held-out-guide Norman extract with known linear-pathway biology, pass-count ≠ 1 after confirmation, or κ of the full state is indistinguishable from a negative-control pair. | `UNIQUE_TARGET` on confirmation clusters; negative-control pair stays `NO_PASS` or curved for the wrong reason. | proposed | 8.0 | active |
| H-002 | Unsupervised *certification* is impossible. Selection is the permanent wall. The honest product is a testability atlas + contract requests, never a DAG. | A procedure, using only rows, that establishes state-independent inclusion at the randomization unit, with a detection floor, on a table whose inclusion *does* depend on X. | `examples/configs/selection_dependent.json` is the constructed lie: declared `state_dependent_unmodeled` blocks four-law. The same CSV under a false `state_independent` declaration is undetectable from rows. | proposed | 20.0 | active |
| H-003 | The binding constraint is **missing corners**, not inference. Most piles are not squares. The high-EV product is a missing-corner / same-target-tilt recommender. | On the next three public extracts (Norman, ALMANAC, one RCT), survey finds complete squares for the scientifically interesting pairs without imputation. | Atlas dominated by incomplete designs and design-extension proposals. | **third_alternative** | 15.0 | active |

H-002 and H-003 can both be true. H-001 can be true for the rare complete square
and still leave H-003 as the product strategy.

## Evidence classes (do not launder)

| Artifact | Role | Disposition | Evidence class |
|---|---|---|---|
| Paper biconditional | THEOREM (assembled object; antecedents cited) | OPEN as a priority claim | in-paper derivations + `mic-core` exact identities. Citations do **not** verify the assembled theorem. Novelty is a separate claim. |
| `κ` on four_law_discrete | EMPIRICAL_PATTERN on a designed fixture | matches closed form log 1.6 / log 0.4 | EXACT_FINITE_COMPUTATION on the fixture |
| Histogram four-law `abstained` | POLICY | intended | construction, not a proof of modularity |
| Survey `proposal_only` | POLICY | intended | must never be upgraded by renaming |
| Norman 2019 as first public square | CONJECTURE about usefulness | OPEN | no extract in-repo |

Law 12: evidence that *selected* a pair (survey, parsimony frontier, DAG learner)
cannot also *test* it. Confirmation clusters or a new experiment only.

Law 13: an apparatus that cannot report “nothing” has not been shown capable of
reporting anything. Empty common support, `NO_PASS`, `MULTIPLE_PASSES`, and
`Underpowered` are load-bearing outputs.

Law 14: a check that cannot fail is not evidence. Running GCM on non-product
quotas is not “a test that always accepts.” The residual product can reject.
It is the **wrong null**: it tests design covariance, not density curvature.
The engine must refuse that track so the wrong null cannot be mistaken for
κ ≡ 0.

**Norman unit.** Published identities files typically expose `gemgroup` /
guide construct, not `replicate_id`. Do not invent a column. Declare the
actual randomization unit, or the survey must serialize
`cluster_unit_basis: row` and stay `proposal_only`.

## Modes of reasoning (lead-agent synthesis, no NTM)

Selected because this is a **scientific audit system**, not a product CRUD app.
Axes that matter: ampliative vs non-ampliative; descriptive vs normative;
belief vs action.

| Mode | What it says about MIC |
|---|---|
| Deductive | The certificate is a biconditional. Software that “passes” without the three conditions is a type error. |
| Causal / deletion | Orientation is two-sample equivalence, not a residual heuristic. |
| Adversarial | The classic attack is swapping four-law and GCM. Keep the tracks unmixable. |
| Abductive | Leftover κ is a *search criterion* for missing measurements, not a DAG edge. |
| Worst-case | Selection, unit mismatch, and aliased partial designs fail closed. |
| Debiasing | Histogram flatness is not modularity. Survey ranking is not orientation. |
| Systems | Packet 1 FrankenPandas must implement `IngestReport`, not a parallel ingest. |
| Counterfactual | If we had treated cells as iid, Norman would look like a certificate and be false. |
| Satisficing | Four-law on estimated quotas is the wedge; do not wait for Packet 3 GCM. |
| Meta | Orchestration (mail, beads, “autonomous mode”) has no mathematical authority. |

Kernel (multiple modes, different reasons): **do not let a crawler issue
`passed`.** Supported: four-law first, GCM later. Disputed: whether H-001 is
worth a Norman extract this week (H-003 says maybe not). Owner-acknowledged:
Franken adapters are markers; Packet 8 is not done.

## Marching orders for the live mail swarm (no NTM)

Roles already present: ProudGull (Packet 1), RoseLark (CLI / proposal),
BlackGoose (autonomous-mode spec + design), Fable agents (paper MDL),
OrangeHill / SandyMill (parallel Grok onboarding).

1. **ProudGull** — implement FrankenPandas *behind* `IngestReport`. Falsifier:
   a Parquet file with a spanning cluster still errors `cluster_spans_regimes`.
2. **RoseLark** — wire `mic ingest|four-law|report|survey` to the engine
   functions. Do not change their authority fields.
3. **BlackGoose** — keep AUTONOMOUS_MODE.md as spec. Do not add a
   `certificate_status` to Stage 0–1. Attack H-001 with a selection-dependent
   counter-table if you want a cheap kill.
4. **Fable / paper** — MDL/parsimony stays proposal language. No “the frontier
   *is* the certificate.”
5. **Anyone extracting Norman** — discovery/confirmation split at the
   construct/`gemgroup` (or a justified biological replicate), never at the
   cell. Alternate guides are confirmation.

Refuter-first. If you file evidence that only *supports* your favorite H,
it does not count until a refuter has been attempted.

## What this slate is not

It is not a BrennerBot workspace, not an NTM pipeline, not a Lean campaign,
and not a 18-section modes report. Those skills are used here as *operators*:
falsifiers, evidence classes, and axis-diverse critique. Standing up the full
workspaces would be process without a new mathematical claim.
