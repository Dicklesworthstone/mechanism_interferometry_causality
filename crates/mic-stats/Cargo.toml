[package]
name = "mic-stats"
description = "Residual-product and two-sample inference primitives for mechanism interferometry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
repository.workspace = true

[features]
default = []
franken = ["dep:fnp-ndarray", "dep:fnp-random", "dep:fsci-stats", "dep:fsci-linalg"]

[dependencies]
mic-core = { path = "../mic-core" }
serde.workspace = true
thiserror.workspace = true
fnp-ndarray = { workspace = true, optional = true }
fnp-random = { workspace = true, optional = true }
fsci-stats = { workspace = true, optional = true }
fsci-linalg = { workspace = true, optional = true }

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
