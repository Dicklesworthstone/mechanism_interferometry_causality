# Paper build

```bash
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
```

The build uses `biblatex` with `biber`. Figures are generated by `../scripts/generate_simulations.py`. The checked PDF has 34 pages and was rendered page-by-page for visual inspection.
