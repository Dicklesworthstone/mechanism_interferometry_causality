# Graduation / cash × training factorial RCT template

Cluster at the unit that was actually randomized (`household_id` or
`village_id`). Person-period rows are measurements.

Use `product_factorial` only after assignment odds at that unit are product.
Otherwise keep `four_law`. Microdata are license-gated; do not copy them
into `site/` or `paper/`.
